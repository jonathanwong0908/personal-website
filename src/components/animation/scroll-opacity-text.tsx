import React from "react";

type ScrollOpacityTextProps = {
  text: string;
};

const ScrollOpacityText = ({ text }: ScrollOpacityTextProps) => {
  return <div>{text}</div>;
};

export default ScrollOpacityText;
